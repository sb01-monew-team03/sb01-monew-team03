spring:
  application:
    name: monew
  profiles:
    active: dev
  datasource:
    driver-class-name: org.postgresql.Driver

logging:
  level:
    root: info
admin:
  email: admin@example.com
  password: supersecret
  nickname: admin